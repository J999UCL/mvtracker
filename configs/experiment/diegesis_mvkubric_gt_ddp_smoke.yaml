# @package _global_
defaults:
  - diegesis_mvkubric_gt_ddp
  - _self_

trainer:
  num_steps: 5
  eval_freq: 10000
  viz_freq: 10000
  save_ckpt_freq: 5

modes:
  validate_at_start: false

logging:
  tags:
    - diegesis-mvkubric-50-50
    - gt-depth
    - ddp2-h100
    - full-model
    - continual-training
    - smoke
